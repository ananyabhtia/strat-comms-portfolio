import type { NextConfig } from "next";

const nextConfig = {
  output: 'export',
  // Optional: If your repo is named something like "portfolio", uncomment the next two lines:
  // basePath: '/portfolio',
  // assetPrefix: '/portfolio/',
};

export default nextConfig;
